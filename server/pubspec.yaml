name: sdui_server
description: Mock backend serving JSON screen contracts for the SDUI Flutter app.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_static: ^1.1.2
